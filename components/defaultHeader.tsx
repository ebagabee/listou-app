import { Text, View } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function DefaultHeader() {
    return (
        <SafeAreaView>
            <View>
                <Text>Header Personalizado aqui (Isso foi o gabe que escreveu nao ia)</Text>
            </View>
        </SafeAreaView>
    )
}